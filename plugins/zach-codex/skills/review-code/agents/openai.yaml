interface:
  display_name: "Review Code"
  short_description: "Review code for subtle maintainability issues"
  default_prompt: "Use $review-code to review these changes for non-linter rule violations and unnecessary complexity."
